[project]
name = "app-server"
version = "0.1.0"
description = "Project application server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "db-util",
]

[tool.uv.sources]
db-util = { workspace = true }
