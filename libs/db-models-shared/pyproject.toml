[project]
name = "db-models-shared"
version = "0.1.0"
description = "Shared models"
readme = "README.md"
authors = [
    { name = "DF" }
]
requires-python = ">=3.12"
dependencies = [
    "db-util",
    "django>=5.1.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
db-util = { workspace = true }
