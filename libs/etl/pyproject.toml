[project]
name = "etl"
version = "0.1.0"
description = "ETL Pipeline"
readme = "README.md"
authors = [
    { name = "DF" }
]
requires-python = ">=3.12"
dependencies = [
    "common-util",
    "db-models-shared",
    "django>=5.1.6",
    "yfinance>=0.2.52",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
common-util = { workspace = true }
db-models-shared = { workspace = true }
