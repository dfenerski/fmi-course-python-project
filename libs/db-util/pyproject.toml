[project]
name = "db-util"
version = "0.1.0"
description = "Database utilities"
readme = "README.md"
authors = [
    { name = "DF" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
